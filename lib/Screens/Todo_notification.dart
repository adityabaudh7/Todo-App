import 'package:flutter/material.dart';

class TodoNotificationScreen extends StatelessWidget {
  const TodoNotificationScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('TodoNotificationScreen'),
      ),
      body: Center(
        child: Text('TodoNotificationScreen'),
      ),
    );
  }
}
